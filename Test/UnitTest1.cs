using WarhauseASP.Server.Service;
using Xunit;
using WarehouseASP.Server;

namespace Test
{
    public class Tests
    {
        [Fact]
        public void GetFileXmlBig_WhenUserIdNotFound_ThrowsIOException()
        {
            //// Arrange
            //FileXml sut = new FileXml(); // Twoja klasa zawieraj¹ca funkcjê GetFileXmlBig
            //var link = "http://example.com/file.xml";
            //var idUser = Guid.NewGuid();
            //var authKey = Guid.NewGuid();
            //var localDir = "path/to/save/file.xml";

            //// Act & Assert
            //var ex = Assert.Throws<IOException>(() => sut.GetFileXmlBig(link, idUser, authKey, localDir));
            //Assert.Equal("No user in DB", ex.Message);
        }
    }
}